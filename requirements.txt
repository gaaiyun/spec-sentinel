# Runtime dependencies. For development also install pytest (see
# pyproject.toml [project.optional-dependencies].dev) or:
#   pip install -e ".[dev]"
# typer >=0.17 vendors its own click and drops the click dependency; cli.py
# imports standalone click directly, so cap typer below that line.
typer>=0.9,<0.17
click>=8.0
pydantic>=2.0
PyYAML>=6.0
